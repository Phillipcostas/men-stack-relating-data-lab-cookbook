<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Community page</title>
</head>
<body>
    <p>Community Page</p>
   
    <ul>
        <% users.forEach((user) => { %>
          <li>
            <%= user.name %>
        </a>
      </li>
    <% }) %>
  </ul>
            <!-- <a href="/users/<%= user._id %>/foods/pantry/<%= pantry._id %>/edit"> -->
           
</body>
</html>